import React from "react"
import { Typography } from "antd"

const Delimiter = () => {
  const { Title } = Typography

  return <Title style={{ textAlign: "center" }}>***</Title>
}

export default Delimiter
